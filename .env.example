# TMDB API Read Access Token (v4 bearer token)
# Sign up at https://www.themoviedb.org/signup
# Go to Settings → API → Create → Developer → copy the "API Read Access Token"
VITE_TMDB_API_TOKEN=your_tmdb_api_read_access_token_here

# If you want to run it on a server, set the variable:
# GitHub Personal Access Token (PAT)
# Sign in to GitHub and go to Settings → Developer settings → Personal access tokens → Fine grained tokens → Generate new token
# Select scope: "Copilot Requests"
GH_Token=your_github_token_here